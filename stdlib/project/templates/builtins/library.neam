module std.project.templates.builtins.library;

pub fun definition() {
  return {
    name: "library",
    description: "Reusable library template",
    files: [
      "neam.toml",
      "src/lib.neam"
    ]
  };
}
