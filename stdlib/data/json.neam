module std.data.json;

pub fun parse(text) {
  return json_parse(text);
}

pub fun stringify(value) {
  return json_stringify(value);
}

// Legacy aliases
pub fun json_parse_text(text) {
  return parse(text);
}

pub fun json_stringify_value(value) {
  return stringify(value);
}
