// Prompt injection probes

module std.agents.redteam.probes.injection;

use std.agents.redteam.probes.registry;

pub fun list_probes() {
  registry.seed_default_probes();
  return registry.list_probes("PromptInjection");
}
