// Code execution probes

module std.agents.redteam.probes.code_exec;

use std.agents.redteam.probes.registry;

pub fun list_probes() {
  registry.seed_default_probes();
  return registry.list_probes("CodeExecution");
}
